{
  "name": "@acme/backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "convex dev",
    "dev:setup": "convex dev --configure --until-success",
    "lint": "run-p -c lint:biome check-types",
    "lint:biome": "biome lint .",
    "check": "run-p -c check:biome check-types",
    "check:biome": "biome check --write .",
    "check-types": "tsc -p convex --noEmit"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@acme/typescript-config": "workspace:*",
    "npm-run-all2": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  },
  "dependencies": {
    "@convex-dev/resend": "catalog:",
    "@workos-inc/node": "^7.74.2",
    "convex": "catalog:"
  }
}
